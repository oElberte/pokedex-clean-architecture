name: pokedex
description: My Pokédex app now with Clean Architecture.
publish_to: 'none' 
version: 0.2.5

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  carousel_slider: ^4.2.1
  http: ^0.13.5
  equatable: ^2.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.3.2
  build_runner: ^2.3.2
  faker: ^2.1.0
  network_image_mock: ^2.1.1
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true